import System.*
import System.Console.*

let a = handle("42")
Console.WriteLine(a)

func handle(text: string) -> Result<int, Exception> {
    // Use a throwing API to demonstrate `(try expression)?`; projected int.Parse returns Result.
    let x = (try Convert.ToInt32(text))?
    WriteLine(x)
    return Ok(x)
}
