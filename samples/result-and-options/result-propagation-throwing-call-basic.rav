import System.*
import System.Console.*

let a = handle("42")
Console.WriteLine(a)

func handle(text: string) -> Result<(), Exception> {
    let x = (try throwing())?
    WriteLine(x)
    return Ok(x)
}

func throwing() {
    throw InvalidOperationException("Too")
}